.SUFFIXES:	.cpp .o

CXX=c++
CXXFLAGS=-I.
CXXLDFLAGS=-G

include makefile.objects


all:libAutumn.so 

.cpp.o:
	$(CXX) $(CXXFLAGS) -c $<

libAutumn.so:$(OBJS)
	 $(CXX) $(CXXLDFLAGS) -o $@ $(OBJS)

clean:
	rm -f *.o ./libAutumn.so

install:

